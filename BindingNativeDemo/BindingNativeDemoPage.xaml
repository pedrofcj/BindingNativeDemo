<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:BindingNativeDemo" x:Class="BindingNativeDemo.BindingNativeDemoPage">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="CenterAndExpand"
            Padding="0, 60"
            VerticalOptions="FillAndExpand">

            <Entry HorizontalOptions="FillAndExpand"
                x:Name="txtNumber01"
                Keyboard="Numeric"
                Placeholder="Enter the first number"/>

            <Entry HorizontalOptions="FillAndExpand"
                Placeholder="Enter the second number"
                Keyboard="Numeric"
                x:Name="txtNumber02"/>

                <Button Text="Add" Clicked="AddButtonClicked" BorderWidth="3" BorderColor="Black" />
                <Button Text="Subtract" Clicked="SubtractClicked" BorderWidth="3" BorderColor="Black" />
                <Button Text="Multiply" Clicked="MultiplyClicked" BorderWidth="3" BorderColor="Black" />
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
